services:
  - type: web
    name: mvc-sqlite-youtube
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
      # Add YOUTUBE_API_KEY manually in Render dashboard (not committed)
